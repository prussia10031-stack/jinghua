# 工作日志

## 2026-06-19 — 阶段一 + 阶段二完成

### 项目初始化
- 使用 `npm create vite@latest reading-app -- --template react` 生成脚手架
- 安装 TailwindCSS v4 (`tailwindcss` + `@tailwindcss/vite`)
- 配置 `vite.config.js` 注册 tailwind 插件
- 替换 `src/index.css` 为 `@import "tailwindcss"`
- 清理 Vite 模板自带文件 (App.css, 示例 assets)

### 阶段一：底层数据流与基础 CRUD

#### 新增文件
| 文件 | 功能 |
|------|------|
| `src/utils/idGen.js` | 生成 `prefix_timestamp_random` 格式的短 ID |
| `src/hooks/useLocalStorage.js` | 通用 LocalStorage 封装 Hook，lazy init + 同步写入 |
| `src/context/DataContext.jsx` | 全局状态管理中心，导出 `DataProvider` + `useData()` |
| `src/components/BookForm.jsx` | 书籍添加表单 |
| `src/components/BookList.jsx` | 书籍列表，含删除按钮 |
| `src/components/ExcerptForm.jsx` | 摘抄添加表单 |
| `src/components/ExcerptList.jsx` | 摘抄列表 |

#### 设计决策
- 状态管理：React Context (DataContext)
- ID 方案：前缀+时间戳36进制+4位随机36进制，零外部依赖
- 表单：原生 HTML 控件

---

### 阶段二：核心交互体验

#### 新增文件
| 文件 | 功能 |
|------|------|
| `src/components/QuickCapture.jsx` | 闪念胶囊 — 全局悬浮快速录入 |
| `src/components/RandomDrop.jsx` | 多巴胺抽取 — 随机反刍摘抄 |
| `src/components/MoodFilter.jsx` | 情绪急救箱 — 快捷筛选 |

---

## 2026-06-19 — 阶段三：数据模型重构 + UI 全面升级

### 数据模型变更 (DataContext)
- ✅ `source` 新增"番茄""豆阅"选项
- ✅ 新增 `orientation` (性向)：BG / BL / 梦向
- ✅ `cp_dynamics` 改名 `content_intro` (内容介绍)
- ✅ 新增 `emotional_conflict` (情感纠葛)
- ✅ 新增 `personal_feelings` (个人感受)
- ✅ 删除 `sugar_level` / `tension_level`
- ✅ 新增 `rating` (0-10，五角星半星评分)
- ✅ 新增 `book_tags` (10 个固定标签多选)
- ✅ LocalStorage key 改为 `reading_books_v2` / `reading_excerpts_v2`（与旧数据不冲突）

### 新增组件
| 文件 | 功能 |
|------|------|
| `src/components/StarRating.jsx` | 五角星半星评分，hover 预览，左半/右半独立点击 |

### 全面改造
| 文件 | 改动 |
|------|------|
| `BookForm.jsx` | 全字段重写 + 新增/编辑双模式 + editTarget prop |
| `BookList.jsx` | 卡片展示新字段 + [编辑]按钮触发编辑模式 |
| `ExcerptForm.jsx` | 新增/编辑双模式 + editTarget prop |
| `ExcerptList.jsx` | [编辑]按钮触发行内编辑 |
| `MoodFilter.jsx` | 完全重写：从 sugar/tension 改为按 book_tags 筛选 |
| `RandomDrop.jsx` | 展示新字段：星评 + 书籍标签 |
| `QuickCapture.jsx` | 样式对齐设计系统 |

### UI 全面升级
- ✅ 引入 **京华老宋体** (标题用)，存储在 `public/fonts/`
- ✅ 左右分栏布局：左侧 240px 边栏 + 右侧自适应内容区
- ✅ 暖米色调色板 (风格参考对齐)：
  - 底 `#e8ded3` / 卡片 `#f4eee7` / 文字 `#333` / 辅助 `#8c857d` / 强调 `#6e4e3b`
- ✅ Brand 卡片：深棕渐变，与风格参考一致
- ✅ 导航项：active 态浅棕底色 + 加粗
- ✅ 底部状态卡片：显示书籍/摘抄计数
- ✅ 所有输入框统一 border 样式，focus 态带 ring
- ✅ 按钮系统：btn-primary (棕底白字) / btn-outline (描边)
- ✅ 全局 Card 风格：大圆角 20px + 柔和阴影
- ✅ 标签 pills：选中深棕填充 / 未选中白底描边

### 编译验证
- `npm run build` — 27 modules, ✓ built in 232ms, 零错误

### 下一步
- 标签管理（自定义标签）
- 搜索/排序功能
- 导入导出 JSON
- 暗色模式

---

## 2026-06-19 — 阶段四：品牌重塑 + 新功能 + 智能解析

### 品牌 & 命名
- ✅ App 名：「镜花」，Brand 卡片：「来如春梦无多时 / 去似朝云无觅处」
- ✅ 导航栏 5 项重命名：书目-春山眉黛 / 摘抄-小池新涨 / 代餐-口嗨拾遗 / 情感-梦湿行云 / 漫游-雪泥鸿爪

### 数据模型变更
- ❌ Book 删除 `emotional_conflict`、`personal_feelings`
- 🆕 Book 新增 `fandom_work`（来源作品）、`cp_name`（CP名称）— 仅 type=同人 时显示
- 🆕 Excerpt 新增 `image` 字段（base64）
- 🆕 SOURCES 重排：`['出版', 'AO3', '晋江', '豆阅', '小红书', 'Lof', '其他']`
- 🆕 Snack 独立数据模型：id, content, image, category(self/others), source, author, tags, created_at
- 🆕 LocalStorage key：`reading_snacks`

### 色调切换
- 从暖棕 (`#6e4e3b`) → Arco Orange Red 系列：
  - `--bg`: `rgb(255, 243, 232)` (level 0)
  - `--primary-dark`: `rgb(204, 81, 32)` (level 6)
  - Brand 渐变：`rgb(247,114,52)` → `rgb(204,81,32)`

### 新功能
| 功能 | 文件 | 说明 |
|------|------|------|
| 星评 5 整星+半星 | StarRating.jsx | 已有，确认正常 |
| 去斜体 | 全局 | 所有摘抄文本移除 italic |
| 自动创建书籍 | ExcerptForm.jsx | 添加摘抄时未选书 → 弹窗快速创建 |
| 摘抄图片 | ExcerptForm/ExcerptList | FileReader→base64，限制 <2MB |
| 智能文本解析 | `src/utils/parseExcerpt.js` | 解析《书名》+作者+笔记数+章节+◆摘抄格式，批量录入 |
| 代餐/口嗨 | SnackForm/SnackList | 独立模块，分类自己/他人，他人可选微博/B站/小红书，支持图片 |

### 编译验证
- `npm run build` — **30 modules**, ✓ built in 268ms, 零错误

### Token 核算
| 活动 | 估算 tokens | DeepSeek API 费用估算 |
|------|------------|---------------------|
| 阶段四全流程（方案+代码+日志） | ~35,000 input + ~8,000 output | ≈ ¥0.03 (输入 ¥0.018 + 输出 ¥0.016) |

> DS API 参考价：输入 ¥0.5/M tokens，输出 ¥2/M tokens。实际消耗以 API dashboard 为准。
